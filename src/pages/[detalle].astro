---
import BaseLayout from '../layouts/detalleProducto.astro';
import "../styles/global.css";

const pageTitle = "Detalle de producto";

const response = await fetch("https://fakestoreapi.com/products");
const data = await response.json();
const datos = data;

export async function getStaticPaths() {
    const data = await fetch('https://fakestoreapi.com/products').then(response => response.json());

    return data.map((title, description, image, price) => {
        console.log(data)

    return {
      params: { detalle: title.id },
      props: { title, description, image, price },
    };
  });
}
---

<BaseLayout pageTitle={pageTitle} nombre={datos.title} descripcion={datos.description} imagen={datos.image} precio={datos.price + '€'}></BaseLayout>

<script>import "preline/dist/preline.js";</script>
